name: Strict mypy fixture annotations

on:
  push:
    paths:
      - .github/workflows/strict-mypy-fixture-annotations.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Annotate pytest fixtures
        run: |